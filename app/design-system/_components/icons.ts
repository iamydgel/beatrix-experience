"use client";

// Re-export icons as client components to avoid passing Server Components
// (lucide icons) as props to the Badge client component.
export { ArrowRight, Sparkles, Target, Trophy } from "lucide-react";